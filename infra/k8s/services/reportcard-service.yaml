apiVersion: apps/v1
kind: Deployment
metadata:
  name: reportcard-service
  namespace: schoolmanage-prod
  labels: { app: reportcard-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: reportcard-service }
  template:
    metadata:
      labels: { app: reportcard-service }
    spec:
      containers:
        - name: reportcard-service
          image: ghcr.io/OWNER/schoolmanage-reportcard-service:latest
          ports:
            - containerPort: 8086
          envFrom:
            - configMapRef: { name: schoolmanage-shared-config }
            - secretRef: { name: schoolmanage-secrets }
          livenessProbe:
            httpGet: { path: /health, port: 8086 }
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /health, port: 8086 }
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: reportcard-service
  namespace: schoolmanage-prod
spec:
  type: ClusterIP
  selector: { app: reportcard-service }
  ports:
    - port: 8086
      targetPort: 8086
