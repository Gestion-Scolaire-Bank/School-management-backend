apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: schoolmanage-prod
  labels: { app: admin-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: admin-service }
  template:
    metadata:
      labels: { app: admin-service }
    spec:
      containers:
        - name: admin-service
          image: ghcr.io/OWNER/schoolmanage-admin-service:latest
          ports:
            - containerPort: 8092
          envFrom:
            - configMapRef: { name: schoolmanage-shared-config }
            - secretRef: { name: schoolmanage-secrets }
          livenessProbe:
            httpGet: { path: /actuator/health, port: 8092 }
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8092 }
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: schoolmanage-prod
spec:
  type: ClusterIP
  selector: { app: admin-service }
  ports:
    - port: 8092
      targetPort: 8092
