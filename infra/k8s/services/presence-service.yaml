apiVersion: apps/v1
kind: Deployment
metadata:
  name: presence-service
  namespace: schoolmanage-prod
  labels: { app: presence-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: presence-service }
  template:
    metadata:
      labels: { app: presence-service }
    spec:
      containers:
        - name: presence-service
          image: ghcr.io/OWNER/schoolmanage-presence-service:latest
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef: { name: schoolmanage-shared-config }
            - secretRef: { name: schoolmanage-secrets }
          livenessProbe:
            httpGet: { path: /health, port: 8084 }
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /health, port: 8084 }
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: presence-service
  namespace: schoolmanage-prod
spec:
  type: ClusterIP
  selector: { app: presence-service }
  ports:
    - port: 8084
      targetPort: 8084
