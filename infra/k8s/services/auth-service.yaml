apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: schoolmanage-prod
  labels: { app: auth-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: auth-service }
  template:
    metadata:
      labels: { app: auth-service }
    spec:
      containers:
        - name: auth-service
          image: ghcr.io/OWNER/schoolmanage-auth-service:latest
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef: { name: schoolmanage-shared-config }
            - secretRef: { name: schoolmanage-secrets }
          livenessProbe:
            httpGet: { path: /actuator/health, port: 8081 }
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8081 }
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: schoolmanage-prod
spec:
  type: ClusterIP
  selector: { app: auth-service }
  ports:
    - port: 8081
      targetPort: 8081
