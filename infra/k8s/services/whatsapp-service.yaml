apiVersion: apps/v1
kind: Deployment
metadata:
  name: whatsapp-service
  namespace: schoolmanage-prod
  labels: { app: whatsapp-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: whatsapp-service }
  template:
    metadata:
      labels: { app: whatsapp-service }
    spec:
      containers:
        - name: whatsapp-service
          image: ghcr.io/OWNER/schoolmanage-whatsapp-service:latest
          ports:
            - containerPort: 8088
          envFrom:
            - configMapRef: { name: schoolmanage-shared-config }
            - secretRef: { name: schoolmanage-secrets }
          livenessProbe:
            httpGet: { path: /health, port: 8088 }
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /health, port: 8088 }
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: whatsapp-service
  namespace: schoolmanage-prod
spec:
  type: ClusterIP
  selector: { app: whatsapp-service }
  ports:
    - port: 8088
      targetPort: 8088
