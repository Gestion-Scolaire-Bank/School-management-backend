apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: schoolmanage-prod
  labels: { app: analytics-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: analytics-service }
  template:
    metadata:
      labels: { app: analytics-service }
    spec:
      containers:
        - name: analytics-service
          image: ghcr.io/OWNER/schoolmanage-analytics-service:latest
          ports:
            - containerPort: 8091
          envFrom:
            - configMapRef: { name: schoolmanage-shared-config }
            - secretRef: { name: schoolmanage-secrets }
          livenessProbe:
            httpGet: { path: /health, port: 8091 }
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /health, port: 8091 }
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: schoolmanage-prod
spec:
  type: ClusterIP
  selector: { app: analytics-service }
  ports:
    - port: 8091
      targetPort: 8091
