apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: schoolmanage-prod
  labels: { app: payment-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: payment-service }
  template:
    metadata:
      labels: { app: payment-service }
    spec:
      containers:
        - name: payment-service
          image: ghcr.io/OWNER/schoolmanage-payment-service:latest
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef: { name: schoolmanage-shared-config }
            - secretRef: { name: schoolmanage-secrets }
          livenessProbe:
            httpGet: { path: /actuator/health, port: 8083 }
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8083 }
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: schoolmanage-prod
spec:
  type: ClusterIP
  selector: { app: payment-service }
  ports:
    - port: 8083
      targetPort: 8083
