apiVersion: apps/v1
kind: Deployment
metadata:
  name: pedagogic-service
  namespace: schoolmanage-prod
  labels: { app: pedagogic-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: pedagogic-service }
  template:
    metadata:
      labels: { app: pedagogic-service }
    spec:
      containers:
        - name: pedagogic-service
          image: ghcr.io/OWNER/schoolmanage-pedagogic-service:latest
          ports:
            - containerPort: 8089
          envFrom:
            - configMapRef: { name: schoolmanage-shared-config }
            - secretRef: { name: schoolmanage-secrets }
          livenessProbe:
            httpGet: { path: /health, port: 8089 }
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /health, port: 8089 }
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: pedagogic-service
  namespace: schoolmanage-prod
spec:
  type: ClusterIP
  selector: { app: pedagogic-service }
  ports:
    - port: 8089
      targetPort: 8089
