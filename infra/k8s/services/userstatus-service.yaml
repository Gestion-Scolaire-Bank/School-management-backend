apiVersion: apps/v1
kind: Deployment
metadata:
  name: userstatus-service
  namespace: schoolmanage-prod
  labels: { app: userstatus-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: userstatus-service }
  template:
    metadata:
      labels: { app: userstatus-service }
    spec:
      containers:
        - name: userstatus-service
          image: ghcr.io/OWNER/schoolmanage-userstatus-service:latest
          ports:
            - containerPort: 8087
          envFrom:
            - configMapRef: { name: schoolmanage-shared-config }
            - secretRef: { name: schoolmanage-secrets }
          livenessProbe:
            httpGet: { path: /health, port: 8087 }
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /health, port: 8087 }
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: userstatus-service
  namespace: schoolmanage-prod
spec:
  type: ClusterIP
  selector: { app: userstatus-service }
  ports:
    - port: 8087
      targetPort: 8087
