apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: schoolmanage-prod
  labels: { app: notification-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: notification-service }
  template:
    metadata:
      labels: { app: notification-service }
    spec:
      containers:
        - name: notification-service
          image: ghcr.io/OWNER/schoolmanage-notification-service:latest
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef: { name: schoolmanage-shared-config }
            - secretRef: { name: schoolmanage-secrets }
          livenessProbe:
            httpGet: { path: /health, port: 8085 }
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /health, port: 8085 }
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: schoolmanage-prod
spec:
  type: ClusterIP
  selector: { app: notification-service }
  ports:
    - port: 8085
      targetPort: 8085
